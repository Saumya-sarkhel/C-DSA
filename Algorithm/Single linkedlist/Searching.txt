Searching: 

Algorithm: Linear_Search ()
Begin.
Step-1: if (head = NULL) then
	Print “Empty list”
	Goto step 7
Step-2: Enter data int key 
Step-3: set flag  0 
Step-4: set p  head 
Step-5: for (p = head; p ≠ NULL; p = pnext)
	if (pdata = key) then
		set flag  1 
		Break
		[end of if]
	[end of for]
Step-6: if (flag = 1) then 
	Print”key found”
	Else print”not found’.
Step-7: Stop.
